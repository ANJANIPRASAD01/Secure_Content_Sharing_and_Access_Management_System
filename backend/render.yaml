services:
  - type: web
    name: filevault-backend
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/filevault-1.0.0.jar
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8080
      - key: JAVA_VERSION
        value: 17
